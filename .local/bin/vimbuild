#!/bin/bash

echo
echo "######################"
echo "#  Instalace vim     #"
echo "######################"
mkdir -p $HOME/.local/src
cd $HOME/.local/src
git clone --depth=1 https://github.com/vim/vim.git
cd vim/src
sudo make install

