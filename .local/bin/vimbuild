#!/bin/bash

echo
echo "######################"
echo "#  Instalace vim     #"
echo "######################"
cd /usr/include
sudo ln -s wayland/* .
mkdir -p $HOME/.local/src
cd $HOME/.local/src
git clone --depth=222 https://github.com/vim/vim.git
cd vim/src
make
sudo make install

